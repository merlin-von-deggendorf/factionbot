# Agent Instructions

When running the bot for testing (e.g., `node index.js`), wait for the
"Logged in as ..." output, then terminate the process immediately. Do not
leave the bot running after a test run.
